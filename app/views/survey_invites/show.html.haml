%p this is the survey invite show view

%p= "Survey: #{@survey_invite.survey.name}"

- @survey_invite.survey_questions.where("position >= ?", @position)
  = cell(SurveyInvite::Cell::SurveyQuestions, @survey_invite, @position)