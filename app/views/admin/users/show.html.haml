%h3= @user.name
%p
  =link_to("👈 Back", admin_users_url, class: "user-back btn btn-secondary")
  = @user.profile_url ? link_to("😊 Profile", @user.profile_url, class: "user-profile-button btn btn-primary") : "🫣 No Profile Yet"
  = @user.chat_url ? link_to("💬 Chat", @user.chat_url, class: "user-chat-button btn btn-primary") : "🤫 Can't Chat Yet"

%table.user.table.table-bordered.table-striped{ data: {url: @update_url, token: @token, id: @user.id } }
  %tbody
    %tr
      %td.header Greeter
      %td.user-greeter= link_to(@user.greeter ? @user.greeter : "I will greet", "#", class: "user-greeter")
    %tr
      %td.header Email
      %td.user-email= link_to @user.email, "mailto:#{ @user.email }"
    %tr
      %td.header Status
      %td.user-status
        %select
          - @options.each do |o|
            - selected = (o == @user.status ? "selected" : nil)
            %option{ selected: selected }= o
    %tr
      %td.header.meeting
        When
        %br
        %span.tzinfo (GMT)
      %td.user-meeting-datetime
        - datetime = @user.welcome_timestamp ? @user.welcome_timestamp.picker_datetime : nil
        %input.datetime-picker.utc-time{ type: "text", value: datetime }
    %tr
      %td.header Shadow
      %td.user-shadow= link_to(@user.shadow_greeter ? @user.shadow_greeter : "I will shadow", "#", class: "shadow-greeter")
    %tr
      %td.header Notes
      %td.user-notes
        %textarea{ cols: "40" }= @user.notes
        %span.save-status
    %tr
      %td.header Questions
      %td.user-questions
        %ol
          - @user.questions_responses.split(" -:- ").each do |qna|
            - q, a = *qna.split("\\")
            %li
              %span.question= q
              %br
              %span.answer= a
        - if @user.status == "Pending"
          = link_to "Approve", |
            admin_approve_user_path(@user.id), |
            class: "btn btn-primary user-approve"
