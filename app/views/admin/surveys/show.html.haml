%h5 Survey Questions

%p.survey-name
  = "Survey: #{@survey.name}"
  = link_to "edit", edit_admin_survey_path(@survey)
  |
  = link_to "notes", admin_survey_notes_path(survey_id: @survey.id)
  |
  = link_to "delete", admin_survey_path(@survey), data: { method: "delete", confirm: "Really delete survey?", token: @token }
  |
  = link_to "test", admin_survey_test_path(@survey)

%table.survey-questions.table.table-bordered.table-striped
  %thead
    %tr
      %th.action Actions
      %th.question-type Question Type
      %th.question Question
      %th.answer-type Answer Type
      %th.has-scale Scale?
  %tbody#sortable
    - @survey_questions.each do |sq|
      %tr.ui-state-default{ data: {url: admin_survey_question_patch_url(sq.id), position: sq.position, id: sq.id, token: @token} }
        %td.actions
          = link_to "edit", edit_admin_survey_survey_question_path(sq.id, survey_id: @survey.id)
          |
          = link_to "del", admin_survey_survey_question_path(sq.id, survey_id: @survey.id), data: { method: "delete", confirm: "Really delete question?" }
        %td.question-type= sq.question_type
        %td.question= sq.question
        %td.answer-type= sq.answer_type
        %td.has-scale= sq.has_scale? ? "Yes" : "No"

= link_to "New Question", new_admin_survey_survey_question_path(@survey.id), class: "btn btn-secondary"
%br
= link_to "Back", admin_surveys_path
